{
  "name": "telegram-bot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node new_backend/index.js",
    "build": "npm --prefix new_backend run build",
    "postinstall": "npm --prefix new_backend install && npm --prefix new_backend run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "fluent-ffmpeg": "^2.1.2",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "stripe": "^14.0.0",
    "telegraf": "^4.16.3"
  }
}
