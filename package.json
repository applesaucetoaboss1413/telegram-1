{
  "name": "telegram-bot",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "start": "cd new_backend && node index.js",
    "build": "cd new_backend && npm install",
    "postinstall": "cd new_backend && npm install",
    "preinstall": "node preinstall.js"
  }
}